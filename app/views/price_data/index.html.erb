<% if logged_in? %>

<div class="margins">
<h2>Price Data</h2>

  <table class="table table-dark table-sm">
    <tr>
      <th>Location</th>
      <th>Low Price</th>
      <th>High Price</th>
    </tr>
    <% @price_data.each do |p| %>
      <% if p.location != "N/A" %>
        <tr>
            <td><%= p.location %></td>
            <td><%= OutputsHelper.convert_format(p.adjusted_low_lot) %></td>
            <td><%= OutputsHelper.convert_format(p.adjusted_high_lot) %></td>
            <td><%= button_to 'Delete Record', p, method: :delete %></td>
        </tr>
      <% end %>

    <% end %>     
  </table>  
</div>
<% else %>
Please Login to View
<% end %>